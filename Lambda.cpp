/*#include <iostream>

/*int main()
{
    auto add = [](int a, int b)
    {
        return a + b;
    };

    std::cout << "SUM: " << add(3, 4) << std::endl; // Output 7
}

int main()
{
    int x = 10;
    int y = 20;

    auto add = [=](int a)
    {
        return x + y + a;
    };

    std::cout
        << "Sum: " << add(5) << std::endl; // Output:Sum:35

    auto addByRef = [&x, &y](int a)
    {
        x = 30; // modifying x
        return x + y + a;
    };

    std::cout << "Sum by reference: " << addByRef(5) << std::endl; // Output: Sum
    // by reference : 55
    std::cout << "x after modification " << x << std::endl; // Output: x after
                                                            // modification

    return 0;
}*/
