#include <iostream>
#include <vector>
#include <algorithm>
int main()
{
    /*{
        std::vector<int> numbers = {1, 2, 3, 4, 5};

        // Using for each with a lambda to print each element
        std::for_each(numbers.begin(), numbers.end(), [](int n)
                      { std::cout << n << " "; });
        std::cout << std::endl;
        return 0;
    }

    std::vector<int> numbers = {3, 6, 2, 6, 7, 3};
    for (int i : numbers)
    {
        std::cout << i << " ";
    }*/

    return 0;
}